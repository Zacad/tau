# TRACKING

| Task | Name | Status | Notes |
|------|------|--------|-------|
| 001 | PI Exploration | DONE | Exploration session on PI architecture |
| 002 | Requirements Definition | DONE | Requirements gathered interactively. TUI deferred. |
| 003 | PI Exploration v2 | DONE | Deep exploration of PI internals mapped to requirements. Go architecture proposal ready.
| 004 | Architecture Design | DONE | Complete architecture with 30 finding review, all Critical/High resolved. 16 decisions documented. |
| 005 | Task Breakdown | DONE | 9 implementation tasks (006–014) defined. Delegate agent review: 2 Critical, 6 High, 8 Medium, 5 Low — all findings incorporated. |
| 006 | Foundation | DONE | types, config, testutil, go.mod. 67 tests pass. go vet/build/mod tidy clean.
| 007 | Provider System | DONE | Provider interface, model registry, auth resolution, OpenAI, Anthropic, Google, OpenAI-compat. 80.7% coverage. 71 tests. go vet/build/mod tidy clean.
| 008 | Tool System | DONE | Tool interface, Registry, 7 built-in tools, file mutation queue, truncation. 75 tests, 83.3% coverage, -race clean.
| 009 | Skills System | DONE | 68 tests, go vet/build/mod tidy clean. Discovery, SKILL.md parser, progressive disclosure, 2 built-in skills. |
| 010 | Subagent System | DONE | Superseded by 047. Original design replaced after deep analysis of OpenCode and PI implementations. See 047 for vertical slice approach. |
| 011 | Session Management | DONE | JSONL storage, lifecycle, auto-naming, compaction. 64 tests, 81.3% coverage, -race clean. |
| 012 | Agent Loop | DONE | Agent struct, state machine, steering/follow-up queues, tool orchestration, context files. 22 tests, -race clean. No subagent dependency.
| 013 | SDK Integration | DONE | Session struct, CreateSession, Prompt/Continue, Steer/Subscribe, Compact, Usage, Model/SetModel, Rename. PI-style model resolution (no ambiguity). 28 SDK tests + 8 provider model resolution tests. go vet/build/mod tidy/-race clean.
| 014 | CLI Analysis | DONE | TUI analysis complete. Compared PI TUI vs opencode vs Go ecosystem. Decision: bubbletea v2. Created 4 implementation tasks (020-023). Analysis docs: TUI-ANALYSIS.md, TUI-LIBRARY-RESEARCH.md.
| 020 | CLI Foundation + Interactive MVP | DONE | All 4 phases complete. Phase 1: CLI skeleton, print/JSON modes, flag parsing, 10 tests, Ollama auto-discovery. Phase 2: TUI core (bubbletea viewport+textarea, event bridge, turn cycle, alt-screen layout, /quit+/help, Ctrl+D, Ctrl+C abort, session flags, deadlock fix). Phase 3: Ctrl+C double-tap to exit, 15 TUI unit tests. Phase 4: E2E verification — tool calls via Ollama confirmed, session resume (-c) verified, ephemeral mode (--no-session) verified. 47 total tests (10 CLI + 15 TUI + 22 other packages). Known issue: gemma4:26b thinking_delta routing → Task 024. Post-MVP fixes: TUI event delivery via `p.Send()` (tea.Batch deadlock), `s.mu` deadlock in event handlers, websearch `q` alias, tool args display, spinner lifecycle. |
| 021 | Message Rendering & Display | DONE | lipgloss styled messages (user/assistant/thinking), tool call rendering (3 states), bubbles/spinner, turn separators, enhanced footer (turns/tokens/cost). 40 tests. Ollama event stream verified. Follow-up session for thinking block whitespace and multi-turn rendering polish. |
| 022 | Session Management UI & Commands | DONE| Session resume picker, model selector, slash commands, skill invocation. **After 020.** |
| 023 | Tab Completion, Testing & Polish | DONE | Tab completion (commands + skills), mock provider injection (--mock/TAU_MOCK_URL), centralized error handling, signal handling (SIGTERM), --help/--version flags, 8 E2E tests, README. All quality gates pass. |
| 024 | Thinking/Reasoning Rendering Investigation | DONE | Root cause: TUI event channel (256 buffer + non-blocking select) drops text_delta events during long thinking blocks. Fix: blocking send + 2048 buffer in model.go. E2E verified: print mode + interactive TUI both show full Polish/English responses. 2 regression tests added.
| 015 | Tool Calling Fix | DONE | OpenAI-compat tool call accumulation, JSON repair, round-trip serialization fix. 16 unit tests. E2E passes with Ollama. Agent loop max-iterations guard. go vet/build/-race clean. |
| 016 | Reasoning Support | DONE | Provider-agnostic reasoning: 3 field names (reasoning_content/reasoning/reasoning_text), block-switching for interleaving, EventThinkingStart/Delta/End, Anthropic signature_delta round-trip. 21 new unit tests. E2E: 1556 bytes reasoning captured from gemma4:26b. go vet/build/-race clean. |
| 017 | Thinking Signature Field | PENDING | Replace NUL-separator with dedicated `Signature` field on `ContentBlock`. Backward-compat migration for existing sessions. **After 016.** |
| 018 | Streaming Block Accumulation | DONE | Fixed O(n²) string concat, text/thinking accumulators with strings.Builder, EventTextStart/End. 4 new unit tests. All existing tests pass. go vet/build/-race clean. |
| 019 | Redacted Thinking | DONE | Handle Anthropic `redacted_thinking` blocks, store encrypted payload for round-trip, display "[Reasoning redacted]". **After 017.** |
| 025 | Rename tau → tau | DONE | Rename project from tau to tau. Module path, binary, config dir, docs, root directory. ~375 references across ~80 files. |
| 026 | Web Search Tool | DONE | websearch + webfetch, SearXNG/Tavily/Brave backends, SSRF, auto-fallback, partial results, Docker SearXNG (8964). 81.1% coverage. E2E verified. Remaining: Researcher subagent integration (deferred to task 010). |
| 027 | Fix SearXNG 400 Error | DONE | Removed stale named volume, strict Available() (200-only), response body in errors, truncateBody helper. 7 new tests. 87 tests total, -race clean. E2E verified. |
| 028 | Styled Markdown Output with Clickable Links | DONE | 028.1: glamour rendering (dark theme, cached, plain text during streaming). 028.2: OSC 8 hyperlinks (code block exclusion, ANSI preservation, www. normalization). 028.4: resize cache invalidation, isFinalized field, edge cases. 028.5: ARCHITECTURE.md TUI section, DECISIONS.md glamour decision. 100+ TUI tests. go vet/build/mod tidy clean. |
| 029 | Streaming Markdown Rendering Exploration | DONE | Exploration complete. Recommended debounced full re-rendering (200ms). Implemented: reusable renderer, cached pending render, change detection, resize invalidation. 16 new tests. go vet/build/mod tidy clean. findings.md + worklog.md created. |
| 030 | Message Queuing | DONE | SDK-level queue (EnqueueMessage/DequeueMessage/PendingCount/OverflowCount). TUI enqueues during streaming, auto-drains on PromptDone. Footer shows queue count. Queued messages visible in viewport. Overflow drops oldest with warning. 4 SDK tests + 8 TUI tests. go vet/build/-race clean. |
| 031 | TUI Performance Fix | DONE | Viewport update throttling (~30fps) during streaming. Finalized block render cache to avoid O(n) re-render per text delta. Ctrl+C abort responsive, scrolling smooth, spinner normal speed. 9 new tests. All 100+ existing tests pass. go vet/build clean. |
| 032 | Command Dropdown Discovery | DONE | Structured Command registry (10 commands with Name/Description/Handler/Available). CommandDropdown component above input (max 7 items, full width, normal border). Opens on "/" with all commands, filters as you type. Arrow navigation, Enter/Tab/Esc handling. Viewport shrinks when dropdown active. Replaced switch statements with registry lookup. 20+ new tests. go vet/build clean. |
| 033 | Fuzzy Command Matching | DONE | Fuzzy subsequence matching with scoring (prefix/consecutive/word-boundary bonuses). Match highlighting in dropdown. 14 new tests. go vet/build clean. |
| 034 | Custom User Commands | DONE | `customcmd` package: discovery (project + global dirs), YAML frontmatter parsing, template substitution ($ARGUMENTS, $1/$2/$3). Registry integration with [custom] indicator in dropdown. /reload command. Embedded command support (priority: project > global > embedded). 22 customcmd tests + 7 integration tests. go vet/build/-race clean. |
| 035 | Multi-Step Interactive Commands | DONE | Custom CommandStep interface + MultiStepRunner. Reusable components: ListStep, InputStep, ConfirmStep, MessageStep. /connect skeleton proof-of-concept. Dropdown → indicator. 37 new tests. go vet/build/-race clean. |
| 036 | TUI Input Bug Fixes | DONE | Three fixes: (1) commands clear input on Enter without inserting newline, (2) model selector Esc/Enter returns focus to input, (3) Ctrl+D exits from idle state regardless of input content. All tests pass. go vet/build clean. |
| 037 | Provider Connection System | IN PROGRESS | 037.1-037.5 DONE. 037.6 (documentation) pending. Build fix: added provider-specific discoverOpenAICompatModels wrappers. |
| 038 | Command Palette Modal | DONE | 038.1-038.11 DONE. Full command palette with Ctrl+P and `/` trigger. 5 reusable components (PaletteList, PaletteInput, PaletteConfirm, PaletteTask, PaletteMessage). Multi-step commands run inside palette. Model selector migrated. CommandDropdown removed. All tests pass. Binary rebuilt. |
| 039 | Fix /connect Model Discovery — Wrong BaseURL | DONE | Fixed BaseURLs for opencode-go (`https://opencode.ai/zen/go/v1`) and opencode-zen (`https://opencode.ai/zen/v1`) in 8 locations. Fixed error classification: disabled models (e.g. glm-5) returned HTTP 401 misclassified as auth_failed — added `ErrorTypeModelUnavailable`. Fixed error block rendering delay: `handleError` now uses `updateViewportWithForce(true)` to bypass throttle/cache. Improved error display: strips internal prefixes ("provider stream error:", etc.) for user-friendly messages. All tests pass. Binary rebuilt. |
| 040 | OpenCode Go Provider Error Handling | DONE | Fixed app hang when weekly limit reached. HTTP streaming, SSE error handling, provider message preservation. All tests pass. Binary rebuilt.
| 041 | OpenCode Zen Provider Error Handling | DONE | Provider-branded error messages, Zen error pattern detection, connection test improvements, 4 E2E tests. All tests pass. Binary rebuilt.
| 042 | OpenCode Zen All Models Support | DONE | ZenProvider wrapper with multi-endpoint routing (gpt→Responses, claude→Messages, gemini→Google, others→Completions). GoogleProvider auth mode. OpenAI Responses API enhanced with thinking + tool call support. Model discovery classifies by ID prefix. 22 new tests. All 100+ tests pass. go vet/build/-race clean. Binary rebuilt.
| 043 | Google Gemini JSON Schema Compatibility | DONE | `sanitizeGoogleSchema()` strips `$schema`/`$id`/`$ref`/`$defs` meta fields, converts integer enums to string enums, filters `required` to existing properties, inlines `$ref` from `$defs`. Fixed type mismatch: `*jsonschema.Schema` → `map[string]any` via marshal/unmarshal. Fixed stream parser: content processing before `FinishReason` check. 7 new tests. All tests pass. Binary rebuilt. Manually verified: basic chat + tool calling (web_search) via `gemini-3.1-pro`. |
| 044 | Zen Provider — Anthropic & OpenAI Error Handling | DONE | Shared `sanitizeToolSchema()` strips `$schema`/`$id`/`$ref`/`$defs`, recursively inlines `$ref` from `$defs`. Applied to Anthropic, OpenAI Responses, OpenAI-compat. OpenAI conditionally excludes `session_usage` from `include` for Zen. OpenAI tools include `type: "function"`. 13 new tests. All tests pass. Binary rebuilt. |
| 045 | OpenRouter Provider Support | DONE | Dedicated OpenRouterProvider, curated model list (20 models), thinking level mapping, provider routing, TUI integration, config extension for user models. 20+ new tests. go vet/build clean. |
| 046 | Thinking Command | DONE | Per-model thinking level control. ThinkingLevelMap on Model struct, agent wiring, SDK per-model persistence, /thinking command with palette selector, footer display, provider-specific mapping (OpenAI effort, Anthropic adaptive/budget, Google level/budget, Ollama thinking flag). 10+ new tests. go vet/build/-race clean. Binary rebuilt. |
| 047 | Subagent System (Vertical Slices) | IN PROGRESS | 047.1 DONE: SubAgent struct, NewSubAgent, Run(), SubAgentResult, fresh context, task sent as user message, 7 tests. 047.11 DONE: SubAgentTool, tool registration, LLM prompt guidance, structured logging. subagent → types + provider (leaf). 047.2 DONE: Timeout + cancellation with context.WithTimeout, DefaultTimeout=5m, Timeout field on SubAgentResult, error classification (timeout vs cancellation), configurable timeout via tool params. 13 subagent tests + 11 tool tests. E2E verified against Ollama (timeout, success, cancellation). 047.3 DONE: ContextFork mode, ParentMessages field, shallow copy of parent messages, context isolation verified (unit + concurrent + E2E), 5 new unit tests + 3 E2E tests. go vet/build/-race clean. 047.4 DONE: Tool integration — local Tool/Executor types to break import cycle, parent hard ceiling validation, tool execution loop (stream→extract→execute→append→repeat), assistant tool_call message appended to conversation before tool results (bug fix), tool usage guidelines in system prompt prevent infinite tool-use loops (bug fix). SubAgentTool passes all parent tools except subagent to child. E2E verified with gemma4:26b + websearch: 2 tool calls, proper answer returned. 24 subagent tests + tools tests. go vet/build/-race clean. 047.5 DONE: 6 built-in agent types with default tool sets, type-based constructor, SubAgentTool type parameter, researcher websearch enhancement. 40 subagent tests. go vet/build/-race clean. 047.6 DONE: Event streaming — Events channel (optional, buffered 256), goroutine forwarder with non-blocking send, all stream event types forwarded (text_delta, thinking_delta, tool_call, done, error), SubAgentID on all events, usage accumulation across tool-use turns. 5 new unit tests + 3 E2E tests. 45 subagent tests total. go vet/build/-race clean. 047.7 DONE: Result injection — LLMVisible field (default true), InjectResult helper for LLM-visible/custom entry modes, FormatForLLM structured output, artifact tracking from write/edit/bash tool calls. 11 new unit tests + 3 E2E tests. 56 subagent tests total. go vet/build/-race clean. 047.8 next: Markdown agent definitions. | 047.10 DONE: Chain execution — RunChain sequential steps with {previous} placeholder substitution, stop-on-failure, usage aggregation, intermediate results tracked. 8 unit tests + 3 E2E tests. go vet/build/-race clean. 047.9 DONE: Parallel execution — RunParallel with concurrency limiting (default 4), max tasks (default 8), semaphore pattern, per-task event channels, result aggregation, order preservation. 12 unit tests + 4 E2E tests. go vet/build/-race clean. 047.8 DONE: Agent Definition Format — Markdown+YAML frontmatter parsing (ParseAgentMD), user/project directory discovery (DiscoverAgents), project>user>builtin override (AllAgents helper), 31 new tests total. SubAgentTool supports agent_name parameter for user-defined agents. SDK discovers agents at session creation. loadProjectAgents bug fix: walk-up order reversed so closest agents override parent ones. Description() dynamically lists user agents with explicit "use agent X" invocation guidance. 6 E2E tests for agent_name execution. /agents TUI command added. go vet/build/-race clean.
